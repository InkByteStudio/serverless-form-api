{
  "name": "serverless-form-api",
  "version": "1.0.0",
  "description": "Serverless contact form API with AWS SAM, Lambda, and DynamoDB",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "deploy": "sam build && sam deploy"
  },
  "keywords": [],
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-ses": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/node": "^25.4.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}
